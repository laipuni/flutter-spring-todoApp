spring:
  datasource:
    url: jdbc:mysql://localhost:3306/todoapp?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: 100101
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

  data:
    redis:
      host: localhost
      port: 6379
